# Production Examples

Application examples are generated from the same tested templates used by the CLI. Run the commands in `docs/EXAMPLES_CATALOG.md` without `--apply` first. This directory also includes a non-executable declarative plugin manifest and a secret-free provider-routing example. Credentials must remain in environment variables or approved secret storage.
