# Administrator Guide

Pin Node.js and package versions, mirror release archives, verify checksums, pre-approve optional CLIs, configure secret-manager/environment references, retain audit logs according to policy, and prohibit project-committed credentials. Use `doctor`, `version`, `status`, `validate`, and `upgrade` for fleet validation.
