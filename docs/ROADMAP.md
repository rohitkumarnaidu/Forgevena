# Roadmap

The high-level architecture remains frozen. Future work is evidence-driven:

1. Address approved audit backlog items without changing safety contracts.
2. Add account-backed compatibility testing in secure CI environments.
3. Evaluate shell completion and machine-readable schemas if user demand justifies them.
4. Improve persistence corruption diagnostics and atomic writes through approved, backward-compatible refactoring.
