# ADR 0005: Frozen Platform Architecture

## Decision

Phases 1–4 reuse the existing CLI, project, module, integration, registry, logging, and template components. New core platform layers require a demonstrated problem and approved ADR.

## Consequences

Feature work favors extension of existing components over new orchestration abstractions.
