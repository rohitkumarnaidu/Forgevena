# Plugin System

Plugins are future versioned packages that implement typed module, detector, template, or provider contracts. A plugin manifest declares capabilities, compatibility, permissions, migrations, and integrity metadata.

Plugins run disabled by default and must not receive secrets unless explicitly granted.
