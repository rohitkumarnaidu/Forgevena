# Template Manager

Templates are versioned, named assets with a compatibility range and variables resolved from project detection. Stack-neutral templates are safe defaults; stack-specific templates require an explicit selection.

Template upgrades must create a diff preview and preserve user modifications.
