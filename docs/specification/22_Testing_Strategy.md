# Testing Strategy

Unit-test detector rules, plans, registry behavior, safety invariants, and adapters. Integration-test disposable repositories across Windows and WSL. Contract-test plugin and provider interfaces.

Release gates include dry-run parity, non-overwrite tests, rollback tests, path-security tests, and documentation checks.
