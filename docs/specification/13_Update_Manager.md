# Update Manager

Updates compare the registry’s module/template versions with available versions, generate a migration plan, and create only missing files by default. Versioned migrations may propose edits, but require explicit confirmation and a backup.

Breaking changes require a major workspace version and documented migration guide.
