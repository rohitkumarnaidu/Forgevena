# Coding Standards

Use small cohesive modules, clear names, structured errors, dependency injection at integration boundaries, and tests for every safety guarantee. Prefer standard-library dependencies where they meet requirements.

All project changes must remain scoped, additive by default, and documented.
