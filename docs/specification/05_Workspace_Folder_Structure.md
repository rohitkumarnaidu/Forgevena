# Workspace Folder Structure

The workspace holds CLI code plus shared `configs/`, `templates/`, `prompts/`, `reference/`, `logs/`, `cache/`, `backups/`, `registry/`, and `updates/` directories. Projects remain in developer-selected locations and own `.ai-workspace/` state.

Reference repositories are never copied into projects automatically.
