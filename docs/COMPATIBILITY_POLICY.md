# Compatibility Policy

Compatibility covers documented CLI contracts, registry migrations, supported Node.js versions, declared operating systems/architectures, and additive project behavior. Third-party provider/CLI compatibility is validated at integration boundaries but remains subject to upstream account and API changes. Existing user files are never overwritten during compatibility migrations.
