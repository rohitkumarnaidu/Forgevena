# Quick Start

Install `forgevena@1.2.1`, run `forgevena doctor`, preview `forgevena create DemoApi --template fastapi --dry-run`, apply the reviewed plan, enter the project, and run `forgevena validate`. For an existing repository, always run `forgevena init --dry-run` before `--apply`. The legacy `ai-workspace` alias remains supported throughout 1.x.
