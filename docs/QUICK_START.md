# Quick Start

Run `ai-workspace doctor`, preview `ai-workspace create DemoApi --template fastapi --dry-run`, apply the reviewed plan, enter the project, and run `ai-workspace validate`. For an existing repository, always run `ai-workspace init --dry-run` before `--apply`.
