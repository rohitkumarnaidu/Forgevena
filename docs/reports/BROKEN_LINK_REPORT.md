# Broken Link Report

Local validation scans every Markdown link and image reference relative to its source document. The scheduled Lychee workflow additionally validates external HTTP links with bounded retries and caching.

Latest local audit: zero broken local links. External-link results are produced by `.github/workflows/broken-links.yml` because they require network access.
