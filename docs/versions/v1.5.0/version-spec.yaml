{
  "version": "v1.5.0",
  "title": "Isolated Plugin and MCP Ecosystem",
  "outcome": "Execute extensions out of process with deny-by-default permissions, signed manifests, bounded resources, and governed source and host adaptation.",
  "owner": "Plugin and MCP Maintainers",
  "roadmapAuthority": "docs/strategy/FORGEVENA_VERSIONED_PRODUCT_ROADMAP.md#8-v150--isolated-plugin-and-mcp-ecosystem",
  "riskTier": "tier-3",
  "dependsOn": [
    "v1.4.0"
  ],
  "blocks": [
    "v1.6.0"
  ],
  "architectureDelta": "Add worker-process JSON-RPC isolation while retaining Forgevena Core as policy, credential, filesystem, network, audit, and lifecycle authority.",
  "interfaceImpacts": [
    "Version plugin manifest, RPC, permission, source-adapter, and host-adapter contracts.",
    "Add lifecycle and SDK commands while preserving declarative plugin compatibility."
  ],
  "nonGoals": [
    "No in-process untrusted execution.",
    "No raw credentials or unrestricted lifecycle scripts for plugins."
  ],
  "metrics": [
    "A failed or malicious plugin cannot crash or corrupt the host.",
    "Every permission denial and lifecycle action is auditable.",
    "Compatibility claims require dated import/export fixtures."
  ],
  "acceptance": [
    "Malformed RPC, hangs, crashes, output floods, dependency cycles, signature failures, and rollback are tested.",
    "Unsigned plugins remain disabled unless explicitly approved.",
    "MCP authentication, transport, capability, and health policies fail closed."
  ],
  "assumptions": [
    "Worker processes provide the initial portable isolation boundary.",
    "Vendor formats remain projections, not canonical package truth."
  ],
  "openQuestions": [],
  "implementationContract": "docs/versions/v1.5.0/implementation-contract.md",
  "features": [
    {
      "id": "isolated-plugin-runtime",
      "title": "Isolated Plugin Runtime",
      "status": "committed",
      "summary": "Run signed plugins in bounded worker processes using versioned JSON-RPC.",
      "value": "Extensions become executable without inheriting host authority.",
      "architecture": "The host brokers scoped filesystem, network, provider, and state operations and terminates non-compliant workers.",
      "dependencies": [
        "v1.4 provider contract",
        "v1.3 policy, vault, state, and audit"
      ],
      "acceptance": [
        "Timeouts, limits, cancellation, and termination pass.",
        "Raw credentials are never exposed.",
        "Rollback restores a verified previous package."
      ],
      "risks": [
        "OS process isolation differs.",
        "RPC compatibility drift."
      ]
    },
    {
      "id": "mcp-source-host-adapters",
      "title": "MCP, Source, and Host Adapter Foundation",
      "status": "committed",
      "summary": "Inspect external sources and translate supported capability bundles through explicit compatibility and loss reports.",
      "value": "Users can assess external capabilities without treating arbitrary packages as trusted Forgevena content.",
      "architecture": "Detect, acquire, quarantine, inspect, normalize, analyze, sandbox-test, package, sign, and install are separate governed stages.",
      "dependencies": [
        "isolated-plugin-runtime"
      ],
      "acceptance": [
        "Loss and permission-delta reports are mandatory.",
        "Unsupported primitives fail visibly.",
        "Quarantine inspection performs no activation."
      ],
      "risks": [
        "Supply-chain malware.",
        "Silent semantic loss during translation."
      ]
    }
  ]
}
