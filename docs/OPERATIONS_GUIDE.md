# Operations Guide

Monitor `doctor`, validation, structured logs, integration health, and registry schema/version. Rotate credentials through the credential lifecycle. On upgrade failure use `upgrade rollback --apply --yes`; use project rollback for managed bootstrap assets. Preserve backups until the release is accepted.
